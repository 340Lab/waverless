{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "type": "shell",
            "command": "cargo build --release"
        },
        {
            "label": "build all",
            "type": "shell",
            "command": "bash scripts/deploy_single_node/run_ans_build.sh"
        },
    ]
}